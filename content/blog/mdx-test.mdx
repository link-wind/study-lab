---
title: "MDX 渲染测试"
description: "测试 MDX 渲染效果，包括代码高亮和数学公式"
date: "2024-03-01"
tags: ["测试", "MDX", "代码高亮"]
image: "/images/test.svg"
---

# MDX 渲染测试

这是一个测试文章，用来验证 MDX 渲染是否正确工作。

## 代码高亮测试

### JavaScript 代码

```javascript
// 测试 JavaScript 代码高亮
function fibonacci(n) {
  if (n <= 1) return n;
  return fibonacci(n - 1) + fibonacci(n - 2);
}

console.log(fibonacci(10)); // 输出 55
```

### Python 代码

```python
# 测试 Python 代码高亮
def quick_sort(arr):
    if len(arr) <= 1:
        return arr
    pivot = arr[len(arr) // 2]
    left = [x for x in arr if x < pivot]
    middle = [x for x in arr if x == pivot]
    right = [x for x in arr if x > pivot]
    return quick_sort(left) + middle + quick_sort(right)

numbers = [3, 6, 8, 10, 1, 2, 1]
print(quick_sort(numbers))
```

### CSS 代码

```css
/* 测试 CSS 代码高亮 */
.container {
  display: flex;
  justify-content: center;
  align-items: center;
  min-height: 100vh;
  background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
}

.card {
  background: white;
  border-radius: 10px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  padding: 2rem;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-5px);
}
```

## 数学公式测试

### 行内公式

这是一个行内数学公式：$E = mc^2$，它应该正确渲染。

### 块级公式

这是著名的欧拉公式：

$$
e^{i\pi} + 1 = 0
$$

二次方程求根公式：

$$
x = \frac{-b \pm \sqrt{b^2 - 4ac}}{2a}
$$

积分公式：

$$
\int_{-\infty}^{\infty} e^{-x^2} dx = \sqrt{\pi}
$$

矩阵表示：

$$
\begin{bmatrix}
1 & 0 & 0 \\
0 & 1 & 0 \\
0 & 0 & 1
\end{bmatrix}
$$

## 表格测试

| 功能 | 状态 | 备注 |
|------|------|------|
| 代码高亮 | ✅ | 支持多种语言 |
| 数学公式 | ✅ | 支持 KaTeX |
| 表格渲染 | ✅ | 支持 GitHub 风格表格 |
| 列表渲染 | ✅ | 支持有序和无序列表 |
| 引用渲染 | ✅ | 支持块级引用 |

## 列表测试

### 无序列表

- 第一项
- 第二项
  - 子项 A
  - 子项 B
- 第三项

### 有序列表

1. 第一步
2. 第二步
   1. 子步骤 1
   2. 子步骤 2
3. 第三步

## 引用测试

> 这是一个块级引用，用来测试引用样式。
> 
> 引用可以包含多行内容，应该正确显示。

## 链接和图片测试

这是一个 [内部链接](/blog)，指向博客列表页面。

这是一个 [外部链接](https://nextjs.org)，指向 Next.js 官网。

## 文本样式测试

**粗体文本** 应该显示为粗体。

*斜体文本* 应该显示为斜体。

~~删除线文本~~ 应该显示删除线。

`行内代码` 应该显示为代码样式。

## 分隔线

---

这是一条分隔线。

## 总结

如果以上所有内容都能正确显示，说明 MDX 渲染系统工作正常！

✅ 代码高亮正常  
✅ 数学公式渲染正常  
✅ 表格渲染正常  
✅ 列表渲染正常  
✅ 引用渲染正常  
✅ 文本样式正常  

---

*最后更新时间：2024年3月1日*